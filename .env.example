# Required
DATABASE_URL=postgresql://user:pass@localhost:5432/mcpgate
PORT=4020

# Optional: Observability
SENTRY_DSN=
OPENMETER_ENABLED=false
OPENMETER_API_KEY=

# Optional: Per-server credentials (env var adapter)
GITHUB_TOKEN=
SLACK_TOKEN=
NOTION_TOKEN=
# ... add tokens for any builtin server you want to use

# Optional: Encrypted DB credential store
CREDENTIAL_ENCRYPTION_KEY=  # 64 hex chars (32 bytes)

# Optional: Admin control plane
ADMIN_API_KEY=
CONTROL_PLANE_PORT=4030
