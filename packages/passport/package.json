{
  "name": "@raijinlabs/passport",
  "version": "0.1.0",
  "description": "Passport matching engine, policy engine, and store — shared across Lucid repos",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src/schemas"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/raijinlabs/lucid-plateform-core.git",
    "directory": "packages/passport"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "json-canonicalize": "^1.0.6",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/uuid": "^9.0.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0"
  }
}